# A/B Test Report

**Winner: `XGBoost`** · Target: `late_delivery_risk` (classification)

## Statistical Results

| Metric | Model A (XGBoost) | Model B (LogisticRegression (baseline)) |
|--------|---------|---------|
| Accuracy | **0.9745** | 0.5302 |
| Delta | +0.4443 | — |
| McNemar p-value | 0.0000 | ✅ Statistically significant (p < 0.05) |
| P(A beats B) Bayesian | **100.0%** | — |
| N test samples | 36,104 | — |

## Visual Analysis
![A/B Test](ab_test.png)

## AI Interpretation

## A/B Test Interpretation: XGBoost vs. Logistic Regression

**Statistical Winner**

XGBoost (Model A) wins this test unambiguously. Across 36,104 test observations, XGBoost achieved 97.45% accuracy versus Logistic Regression's 53.02% — a raw delta of 44.43 percentage points. McNemar's test and chi-square distribution tests both return p-values of effectively 0.0, and the posterior probability that Model A beats Model B is 1.0. There is zero statistical ambiguity here. At this sample size, even a 1–2 point difference would likely be significant; a 44-point gap is not a close call by any measure.

**Practical Business Significance**

A 44-point accuracy lift on late delivery risk classification translates directly into material business value on a platform processing 285M user events. Logistic Regression at 53.02% is barely outperforming random guessing on a binary classification task, meaning the baseline model is functionally useless for operational decisions — misclassifying nearly half of all cases. XGBoost at 97.45% enables reliable, actionable predictions: logistics teams can proactively flag at-risk orders, reduce customer churn from poor delivery experiences, and optimize fulfillment resource allocation. The caveat worth flagging is that accuracy alone can be misleading if the target class is heavily imbalanced — precision, recall, and F1 on the minority class (high-risk deliveries) should be verified before full deployment, as 97.45% accuracy could partially reflect class imbalance rather than true discriminative power.

**Recommendation: Deploy Model A — but validate on operational metrics first**

Deploy XGBoost. The statistical and practical case is overwhelming, and continuing to run a longer test would be both unnecessary and costly, as every day the Logistic Regression baseline is in production represents a significant loss in prediction quality. However, before full rollout, confirm that XGBoost's performance holds on the minority class specifically (pull precision/recall/AUC-ROC), check for feature leakage that might artificially inflate the 97.45% figure, and establish a real-time monitoring baseline for model drift. If those checks pass, this is a straightforward deployment decision.

---
*Generated by Auto Data Scientist v8 — A/B Testing Agent*
